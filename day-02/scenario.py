"""День 02 — split: свободный ответ vs. формат + max_tokens + stop.

Три сценария, по одному на параметр, в порядке пунктов задания:

    day-02-format  — response_format={"type": "json_object"}
    day-02-length  — max_tokens
    day-02-stop    — stop

База у всех трёх общая: одна модель и один и тот же вопрос — дословно один текст
на все шесть колонок дня. Так требует задание: «отправьте один и тот же запрос,
но добавьте формат / ограничение длины / условие завершения», то есть разницу
обязаны давать только параметры. В каждом сценарии левая колонка не задаёт ни
одного параметра, правая меняет ровно один — поэтому разницу можно честно
приписать этому параметру, а не промпту, не модели и не сумме эффектов.

Вопрос — сравнение трёх каналов доставки уведомлений по пяти величинам. Он выбран
так, чтобы структура была естественной формой ответа: три канала × пять величин
одинаково ложатся и в прозу, и в массив объектов. Обе колонки отвечают на одно
и то же, и различается только упаковка.

Маркер <<<КОНЕЦ БЛОКА>>> после каждого разобранного канала модель печатает по
прямой инструкции из промпта. На нём стоит стоп-строка сценария day-02-stop.
Инструкция дана явно, а не через расчёт на определённую разметку списка: иначе
маркер мог бы не встретиться в выводе вовсе, и параметр не сработал бы.

Двум другим сценариям маркер не мешает: max_tokens режет текст по счётчику
токенов раньше, чем модель дойдёт до конца первого канала, а response_format
гарантирует валидный JSON-объект — и форму этого объекта определяет параметр,
а не разметка из промпта. Напечатает ли модель маркер внутри JSON и куда его
положит, не гарантирует никто, и это нормально: сравниваем текст против
структуры, а не разметку.

Подводные камни, каждый ломает свой сценарий молча:

* response_format={"type": "json_object"} возвращает 400 на моделях семейства
  OpenAI, если в самом тексте промпта не встречается слово «JSON». Здесь оно есть:
  вопрос прямо оговаривает, что вернуть, если ответ запрошен в формате JSON.
  Это не стилистика, а условие работоспособности сценария.
* json_object гарантирует валидный JSON-**объект**, не массив. Поэтому промпт
  просит объект с ключом channels, а не массив верхним уровнем: иначе просьба
  из промпта и ограничение параметра тянули бы в разные стороны.
* stop не даёт сигнала в finish_reason: он остаётся stop и со стоп-строкой, и без
  неё. Показывать эффект приходится содержанием, а не статусом.
* Стоп-строка в вывод не попадает — обрыв происходит перед ней.
"""

from __future__ import annotations

from app.schema import Scenario, Session

# Одна модель на все шесть колонок дня. openai/gpt-4o-mini перечисляет
# response_format, max_tokens и stop в supported_parameters, поэтому проходит
# фильтр дропдауна и не отваливается на provider.require_parameters=true,
# который стенд шлёт на каждом вызове.
MODEL = "openai/gpt-4o-mini"

# Единственный вопрос дня: он уходит во все шесть колонок дословно одинаковым.
# Четыре требования к нему, и каждое обязательное — вопрос подобран так, чтобы
# выполнить их все сразу, а не по одному на сценарий.
#
# 1. Слово «JSON» в тексте. Без него response_format={"type": "json_object"}
#    вернёт 400 на моделях семейства OpenAI, и сценарий day-02-format умрёт.
# 2. Длинный свободный ответ. Три канала по пять величин с пояснениями — заведомо
#    больше сотни токенов, поэтому max_tokens=80 обрывает ответ гарантированно,
#    а не «как повезёт».
# 3. Одинаковое содержание в обеих формах. Пять величин по трём каналам ложатся
#    и в прозу, и в массив объектов — сравнивать есть что.
# 4. Маркер, который модель печатает по прямой инструкции, а не по случайности
#    форматирования. На нём стоит стоп-строка day-02-stop.
#
# Про порядок инструкций внутри промпта. Маркер обязан появиться не раньше, чем
# закончится первый содержательный блок, иначе колонка B в day-02-stop оборвётся
# на пустом месте. Поэтому в тексте сразу три страховки: запрет на вступление
# и на перечисление каналов заранее, требование назвать все пять величин до
# маркера и прямой запрет ставить маркер перед первым каналом.
QUESTION = (
    "Сравни три канала доставки уведомлений: push, email и SMS.\n"
    "По каждому каналу назови ровно пять величин: стоимость за 1000 сообщений, "
    "типичную задержку доставки, долю прочтений, главный риск и когда этот канал "
    "стоит выбирать.\n"
    "Если ответ запрошен в формате JSON, верни объект с единственным ключом "
    "channels — массивом из трёх объектов с ключами channel, cost_per_1000, "
    "latency, read_rate, main_risk, when_to_use. Иначе отвечай обычным текстом: "
    "абзац на канал, каждая величина с подписью, что это.\n"
    "Разбирай каналы в порядке push, email, SMS. Не пиши вступления и не "
    "перечисляй каналы заранее — начинай сразу с разбора push.\n"
    "Закончив очередной канал — и только после того, как назвал по нему все пять "
    "величин, — выведи отдельной строкой маркер <<<КОНЕЦ БЛОКА>>>. Копируй его "
    "посимвольно, без кавычек и без markdown-разметки. Перед первым каналом "
    "маркера быть не должно."
)

MESSAGES = [{"role": "user", "content": QUESTION}]

# Заведомо меньше того, что модель хочет написать на этот вопрос: обрыв
# гарантирован, а не «как повезёт». Первый канал в 80 токенов не укладывается,
# поэтому до маркера генерация не доходит и сценарии не мешают друг другу.
LIMIT = 80

# Стоп-строка = маркер конца блока, который промпт диктует дословно. Ответ
# оборвётся на границе после первого канала, самой строки в выводе не будет.
STOP = ["<<<КОНЕЦ БЛОКА>>>"]

FORMAT = Scenario(
    id="day-02-format",
    title="Формат ответа",
    description=(
        "Одна модель, дословно один и тот же вопрос в обеих колонках: сравнить "
        "push, email и SMS по пяти величинам. "
        "Левая колонка не задаёт ни одного параметра. Правая добавляет ровно один — "
        "response_format={'type': 'json_object'}; лимита длины здесь нет специально, "
        "чтобы JSON пришёл целым и валидным. "
        "Вопрос выбран так, что структура — естественная форма ответа: три канала "
        "× пять величин одинаково ложатся и в прозу, и в массив объектов. "
        "Поэтому колонки отвечают на одно и то же, и сравнивать есть что. "
        "Смысл сценария: response_format меняет форму ответа и только форму — "
        "ни содержание, ни то, как ответ завершается, от него не зависят."
    ),
    watch_for=(
        "Содержание: и слева, и справа те же пять величин по тем же трём каналам — "
        "стоимость за 1000, задержка, доля прочтений, главный риск, когда выбирать. "
        "Меняется упаковка, а не ответ: одно содержание в двух формах. "
        "Форма: слева проза абзацами, справа JSON-объект с массивом channels и "
        "полями cost_per_1000, latency, read_rate, main_risk, when_to_use. "
        "Схему объекта диктует не промпт: параметр гарантирует валидный JSON, "
        "а не конкретные ключи — модель вправе назвать их иначе или вложить глубже. "
        "Маркер «<<<КОНЕЦ БЛОКА>>>» справа может не появиться вовсе: отдельной строкой "
        "его в JSON не выведешь, и это ещё одна иллюстрация к уроку — формат задаёт "
        "параметр, а просьба в промпте остаётся просьбой. "
        "finish_reason: stop в обеих колонках — формат не про завершение. "
        "JSON справа дописан до закрывающей скобки: лимита нет, обрывать нечему."
    ),
    layout="split",
    # Вопросы задания дня: «сравните ответы — без ограничений и с ограничениями».
    # Здесь ограничение одно, response_format, поэтому судью спрашиваем про форму,
    # про сохранность содержания и про прикладной вывод. Всё это он видит: ему
    # уходят полные тексты обеих колонок и их параметры.
    judge_questions=[
        "Чем два ответа отличаются по форме?",
        "Одинаковое ли у них содержание: на все ли пять величин по всем трём "
        "каналам отвечает каждая колонка?",
        "Для каких задач уместнее свободный текст, а для каких строгий JSON?",
    ],
    sessions=[
        Session(
            label="A. Свободный ответ",
            model=MODEL,
            messages=MESSAGES,
            note=(
                "Ни одного параметра запроса: «свободный» здесь — про запрос, а не "
                "про требования к форме. Форму задаёт только промпт, и он просит "
                "прозу — абзац на канал. Заканчивает ответ модель сама: "
                "finish_reason = stop."
            ),
        ),
        Session(
            label="B. + response_format",
            model=MODEL,
            messages=MESSAGES,
            response_format={"type": "json_object"},
            note=(
                "Единственное отличие — response_format={'type': 'json_object'}. "
                "Тот же вопрос, та же модель, длину никто не ограничивает: JSON "
                "приходит целым. Работает только потому, что слово «JSON» есть "
                "в самом промпте."
            ),
        ),
    ],
)

LENGTH = Scenario(
    id="day-02-length",
    title="Ограничение длины",
    description=(
        "Одна модель, дословно один и тот же вопрос в обеих колонках: сравнить "
        "push, email и SMS по пяти величинам. "
        "Левая колонка не задаёт ни одного параметра. Правая добавляет ровно один — "
        f"max_tokens={LIMIT}; формат ответа не трогаем. "
        f"{LIMIT} токенов не хватает даже на первый канал из трёх, поэтому обрыв "
        "гарантирован, а не «как повезёт». "
        "Смысл сценария: max_tokens — единственный из трёх параметров дня, "
        "который виден сразу по статусу, а не по содержанию."
    ),
    watch_for=(
        "finish_reason: слева stop, справа length — прямой сигнал, что ответ "
        "обрезали снаружи, а не модель закончила сама. "
        f"completion_tokens справа упирается ровно в {LIMIT}, слева в несколько раз больше. "
        "Содержание: слева все три канала, справа обрыв внутри первого — до email "
        "и SMS дело не дошло, фраза оборвана на полуслове, никакого «в заключение». "
        "Маркера «<<<КОНЕЦ БЛОКА>>>» справа тоже нет: до конца первого блока "
        "генерация не дожила. "
        "Три независимых подтверждения одного ограничения: статус, счётчик и сам текст."
    ),
    layout="split",
    # Судья видит finish_reason и число токенов в ответе по каждой колонке —
    # значит, третий вопрос про разумный лимит тоже отвечаемый, а не гадание.
    judge_questions=[
        "Чем ответ с ограничением длины отличается от ответа без него — "
        "по объёму и по содержанию?",
        "Что показывает finish_reason в каждой колонке и о чём говорит разница?",
        "Какой лимит стоило бы поставить, чтобы ответ на этот вопрос остался "
        "законченным?",
    ],
    sessions=[
        Session(
            label="A. Свободный ответ",
            model=MODEL,
            messages=MESSAGES,
            note=(
                "Ни одного параметра запроса. Объём задан только просьбой в промпте — "
                "три канала по пять величин, — и модель дописывает ответ до конца "
                "сама: finish_reason = stop. Просьбу можно выполнить или не выполнить; "
                "справа тот же объём режут снаружи, не спрашивая."
            ),
        ),
        Session(
            label="B. + max_tokens",
            model=MODEL,
            messages=MESSAGES,
            max_tokens=LIMIT,
            note=(
                f"Единственное отличие — max_tokens={LIMIT}. Генерацию обрывают жёстко "
                "по счётчику токенов: finish_reason = length, фраза оборвана на полуслове "
                "где-то внутри первого канала."
            ),
        ),
    ],
)

STOP_SCENARIO = Scenario(
    id="day-02-stop",
    title="Условие завершения",
    description=(
        "Одна модель и тот же общий вопрос дня, что в двух других сценариях: "
        "сравнить push, email и SMS по пяти величинам, а после каждого разобранного "
        "канала вывести отдельной строкой маркер «<<<КОНЕЦ БЛОКА>>>». "
        "Левая колонка не задаёт ни одного параметра. Правая добавляет ровно один — "
        "stop=['<<<КОНЕЦ БЛОКА>>>']. "
        "Маркер модель печатает по прямой инструкции из промпта, а не по случайности "
        "форматирования: за то, сработает параметр или нет, отвечает текст задания, "
        "а не удача. "
        "Смысл сценария: stop отработает — и не оставит следа в статусе ответа."
    ),
    watch_for=(
        "Содержание: слева все три канала, каждый закрыт строкой «<<<КОНЕЦ БЛОКА>>>»; "
        "справа только push — на первом же маркере генерацию остановили, email и SMS "
        "просто нет. Разница читается с одного взгляда, без счётчиков. "
        "Самого маркера справа в выводе НЕТ: обрыв происходит перед стоп-строкой, "
        "провайдер её не отдаёт. Слева маркеры видны все — там параметра нет. "
        "finish_reason: stop в ОБЕИХ колонках — статус одинаков и там, где модель "
        "договорила сама, и там, где её остановила стоп-строка. "
        "Это и есть урок дня: параметр сработал, а по статусу этого не понять — "
        "только по длине и содержанию. Сравните с day-02-length, где max_tokens "
        "заявляет о себе сразу: finish_reason = length."
    ),
    layout="split",
    # Главный вопрос дня — второй: finish_reason у обеих колонок одинаковый, и
    # судью не подводят к ответу, а просят проверить, хватает ли этого поля.
    # Третий вопрос отвечаем по полным текстам: слева маркеры есть, справа обрыв
    # происходит перед стоп-строкой, и её в выводе не остаётся.
    judge_questions=[
        "Чем два ответа отличаются по содержанию: сколько каналов разобрано "
        "в каждом?",
        "Что показывает finish_reason в каждой колонке — и достаточно ли одного "
        "этого поля, чтобы понять, что ответ оборвали снаружи?",
        "Встречается ли в самих ответах строка «<<<КОНЕЦ БЛОКА>>>» и где именно?",
    ],
    sessions=[
        Session(
            label="A. Свободный ответ",
            model=MODEL,
            messages=MESSAGES,
            note=(
                "Ни одного параметра запроса. Модель проходит все три канала и после "
                "каждого печатает «<<<КОНЕЦ БЛОКА>>>», как просил промпт, — "
                "и заканчивает сама: finish_reason = stop."
            ),
        ),
        Session(
            label="B. + stop",
            model=MODEL,
            messages=MESSAGES,
            stop=STOP,
            note=(
                "Единственное отличие — stop=['<<<КОНЕЦ БЛОКА>>>']. Генерация "
                "останавливается на первом же маркере: остаётся один канал из трёх, "
                "самого маркера в выводе нет, а finish_reason всё тот же stop."
            ),
        ),
    ],
)

# Порядок значим: он задаёт порядок в сайдбаре и совпадает с порядком, в каком
# параметры перечислены в задании дня — response_format, max_tokens, stop.
SCENARIOS = [FORMAT, LENGTH, STOP_SCENARIO]
