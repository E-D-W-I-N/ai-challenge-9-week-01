"""День 04 — одна модель, один промпт, три температуры: 0.0 / 0.7 / 1.2.

Три колонки отличаются ровно одним числом, и в каждой repeats=5: пять прогонов
одного и того же запроса. Пятнадцать вызовов за сценарий.

Повторы здесь не роскошь, а условие работоспособности дня. Температура —
свойство статистическое: она задаёт, насколько широко модель сэмплирует из
распределения, и на одном ответе этого не увидеть. Прогоны дают выборку
напрямую, а стенд считает под колонкой долю уникальных ответов.

Прогонов именно пять, а не три. Шкала «N из 3» — это четыре деления на всю
картину, соседние колонки то и дело попадают в одно. Пятёрка вдвое подробнее,
и различие 1/5 → 3/5 → 5/5 читается сразу. Вместе с ней закреплён конкретный
эндпоинт провайдера: см. PIN_PROVIDER ниже.

Отсюда требования к промпту, и они противоположны прежним. Раньше нужен был
ответ, внутри которого виден разброс (восемь ассоциаций плюс проверяемый факт).
Теперь разброс считается МЕЖДУ прогонами, а сам ответ должен быть коротким и
сравнимым: уникальность стенд определяет точным совпадением текста после
strip() — ни регистр, ни пунктуация, ни кавычки не снимаются, — так что любая
болтовня вокруг ответа портит счётчик. Поэтому формат зажат до двух строк без
преамбул и нумерации, а обрамление запрещено прямо: «без кавычек» в первой
строке и «без точки» во второй. Это не вкусовщина: одно и то же название,
написанное без обрамления, в лапках и в ёлочках, даёт счётчику три уникальных
ответа, а 2007 и 2007. — два. Бьёт это ровно по средней колонке: на t=0
декодирование жадное и обрамление стабильно, на t=1.2 и так 5 из 5, а t=0.7
из-за такой мелочи уезжает в потолок и склеивается с правым соседом.

Ответ устроен так, чтобы день по-прежнему показывал все три оси задания:

  * первая строка — название кофейни на маяке. Задача открытая, одинаково
    допустимых ответов много: отсюда и разнообразие (совпадают ли прогоны),
    и креативность (насколько названия неожиданны);
  * вторая строка — год выхода первого iPhone. Правильный ответ ровно один и
    общеизвестен. Это опора: видно, чем платят за разнообразие, когда на t=1.2
    вместе с фантазией разъезжается и факт.

Первая строка даёт счётчику уникальности почти всю его работу: год при любой
температуре чаще один и тот же, а название — то, что реально расходится.

Пространство ответа при переходе на пять прогонов оставлено прежним, и это
выбор, а не инерция. Средняя колонка — единственная, на которую промпт вообще
влияет (слева жадное декодирование, справа хвост распределения получает шанс
при любой формулировке), и читаемость дня держится именно на ней. Задача
с одним явным фаворитом и длинным хвостом — как раз то, что даёт на t=0.7
среднее значение счётчика; расширь пространство до равновероятного (три
названия вместо одного, поле без якоря) — и 0.7 уедет в 5/5 к соседу справа,
а монотонность, ради которой день и сделан, пропадёт.

Запреты на кавычки и точку идут в другую сторону и потому не спорят с этим:
они не добавляют вариантов, а убирают из счётчика источник уникальности,
который к температуре отношения не имеет.

Модель по умолчанию — не anthropic/*: это семейство обрезает температуру на 1.0
и вернуло бы 400 на колонке t=1.2. Из дропдауна стенд их убирает сам (в сценарии
есть температура выше 1.0), но дефолт обязан быть безопасным и без дропдауна.

Рассуждающие модели дропдаун НЕ убирает (фильтр смотрит только на temperature,
:free и anthropic/*), а температуру они игнорируют молча — переключать на них
колонку нельзя. В дропдауне этого дня они составляют большинство: 167 моделей
из 278. По имени они не опознаются: у deepseek-r1 и
gemini-*-flash нет -thinking в id, а o-серия OpenAI, наоборот, до дропдауна
этого дня не доходит — она не заявляет temperature. Надёжный признак — reasoning
в supported_parameters; список проверенно чистых моделей и разбор —
в day-04/README.md.

seed сознательно НЕ фиксируется. С повторами это уже не мелочь: один seed на всю
серию схлопнул бы колонку t=1.2 к одному ответу, то есть убил бы ровно тот
эффект, ради которого день и сделан.
"""

from app.schema import Scenario, Session

MODEL = "openai/gpt-4o-mini"

# Эндпоинт, которому отданы все пятнадцать вызовов. У openai/gpt-4o-mini в
# каталоге OpenRouter их три: openai, azure и azure/swedencentral. Берём
# первоисточник — openai: это вендор самой модели, и он единственный из трёх
# заявляет max_tokens (у обеих азуровских точек вместо него
# max_completion_tokens), так что при require_parameters, который стенд ставит
# на каждом вызове, выбор всё равно сводится к нему.
#
# Здесь именно ТОЧНЫЙ ТЕГ эндпоинта, а не имя провайдера: OpenRouter понимает
# слаг без суффикса как «все эндпоинты этого провайдера» (deepinfra матчит и
# deepinfra/fp8, google-vertex — все регионы), а дню нужен один. У вендорского
# эндпоинта gpt-4o-mini тег так и выглядит — openai, без суффикса; у запасных
# моделей он с суффиксом, таблица пар в day-04/README.md.
PROVIDER = "openai"

PROMPT = (
    "Ответь ровно двумя строками, без пояснений и без нумерации.\n"
    "Строка 1: название для кофейни на маяке — 1–3 слова, без кавычек.\n"
    "Строка 2: год выхода первого iPhone — только число, без точки."
)


def _messages() -> list[dict]:
    """Один и тот же промпт для всех трёх колонок — различие только в temperature."""
    return [{"role": "user", "content": PROMPT}]


# Три колонки должны отличаться температурой и ничем больше. Здесь два поля, и
# они делают разное. allow_fallbacks запрещает уход к другому провайдеру, order
# называет конкретного. Одного запрета мало: без order «не уходить» значит лишь
# «остаться у того, кого выбрал роутер», а выбрать он мог любой из трёх
# эндпоинтов модели — с другой квантизацией и другой версией рантайма. Тогда
# часть разброса приходит не от температуры, и колонка t=0 показывает
# уникальность просто потому, что её прогоны обслужили разные машины.
#
# Плата за жёсткость двойная. Прогон упадёт, если эндпоинт openai недоступен, —
# тихого фолбэка не будет. И, что важнее, закрепление привязано к модели: llama
# и mistral у провайдера openai не обслуживаются, так что смена модели
# в дропдауне уронит все три колонки. Менять её только здесь, парой
# MODEL + PROVIDER; готовые пары и разбор — в day-04/README.md.
PIN_PROVIDER = {"provider": {"order": [PROVIDER], "allow_fallbacks": False}}

# Две короткие строки — это ~15 токенов. 80 берём с запасом на t=1.2, где модель
# охотнее добавляет лишнее. Обрыв по длине — эффект Дня 2, здесь он только
# запутал бы, а ещё испортил бы счётчик: обрезанный ответ отличается от целого
# и считается уникальным.
MAX_TOKENS = 80

# Пять прогонов на колонку. Тройка — это шкала из четырёх делений (1, 2, 3
# из 3), и соседние колонки на ней постоянно совпадают: счётчик двигался, а
# разница между температурами не читалась. Пятёрка различает колонки вдвое
# подробнее, и типичная картина 1/5 → 3/5 → 5/5 видна с одного взгляда.
# Платим пятнадцатью вызовами вместо девяти — цена растёт кратно.
REPEATS = 5


SCENARIOS = [
    Scenario(
        id="day-04-temperature",
        title="temperature 0 / 0.7 / 1.2",
        description=(
            "Одна модель, один промпт, пять прогонов в каждой колонке. Отличается "
            "ровно одно число — temperature: 0.0, 0.7, 1.2. Ответ короткий и "
            "жёстко форматированный: первая строка — придуманное название "
            "кофейни на маяке, вторая — год выхода первого iPhone. Под каждой "
            "колонкой стенд считает долю уникальных ответов по пяти прогонам: "
            "это и есть мера разброса, которую даёт температура."
        ),
        watch_for=(
            "Главное здесь — строка «уникальных ответов: N из 5» под каждой "
            "колонкой. Слева она обычно 1 из 5, справа 5 из 5, посередине как "
            "получится — и именно средняя колонка показывает, что температура "
            "это ручка, а не выключатель. Сами прогоны: слева пять одинаковых "
            "названий, справа пять разных и заметно более вычурных, иногда с "
            "нарушенным форматом. Вторая строка — проверка на точность: эталон "
            "2007, и именно справа выше шанс увидеть другой год или лишние "
            "слова вместо голого числа. Разнообразие и ошибки растут вместе, "
            "это одна ручка, а не две. Пятнадцать вызовов вместо девяти: "
            "колонки заполняются заметно дольше. Промпт запрещает кавычки "
            "вокруг названия и точку после года — счётчик сравнивает строки "
            "дословно, и «Маяк и Зерно» против «\"Маяк и Зерно\"» дали бы ему два "
            "уникальных ответа при одном названии. Если формат всё же сломался "
            "(вероятнее всего справа, где он и ломается), эта единица "
            "уникальности пришла не от температуры. Оговорка, о которой стоит "
            "помнить: 1 из 5 при t=0 вероятна, но не гарантирована. Провайдер "
            "закреплён, и роутинг как источник разброса убран, но батчинг на "
            "стороне провайдера остаётся — 2 из 5 слева это не поломка "
            "сценария, разбор в day-04/README.md. Модель в дропдауне НЕ "
            "переключайте, и сразу по двум причинам. Первая: все вызовы "
            "закреплены за провайдером openai, а он не обслуживает ни llama, ни "
            "mistral — колонки просто упадут. Вторая: рассуждающие модели "
            "игнорируют temperature молча, и день покажет неправду, а в "
            "дропдауне Дня 04 они большинство и по имени не опознаются. Нужна "
            "другая модель — правьте в day-04/scenario.py пару MODEL + PROVIDER "
            "(готовые пары в day-04/README.md) и обновите страницу."
        ),
        layout="split",
        # Пункты задания Дня 4: «сравните ответы по точности, креативности,
        # разнообразию» — три оси, по вопросу на каждую, — и «сформулируйте,
        # для каких задач лучше подходит каждая настройка».
        #
        # Судья видит все пять прогонов каждой колонки и долю уникальных, поэтому
        # первый вопрос спрашивает именно про серию, а не про один ответ.
        #
        # Эталон фактической строки судье намеренно не сообщается — ни здесь,
        # ни в description, который стенд отправляет ему вместе с вопросами.
        # Год общеизвестен, судья крупнее подопытных: пусть проверит сам, иначе
        # вопрос про точность проверяет не модель, а нас. (watch_for судье
        # не уходит, там эталон назван прямо — это заметка для автора дня.)
        judge_questions=[
            "Насколько пять прогонов внутри каждой колонки похожи друг на друга "
            "и как доля уникальных ответов меняется от колонки к колонке?",
            "Что стоит во второй строке каждого прогона и какие из этих "
            "ответов на фактический вопрос верны?",
            "Насколько предсказуемы или неожиданны названия в первой строке "
            "и встречаются ли среди них бессмысленные или ломающие формат?",
            "Для каких задач подходит каждая из трёх температур, если судить "
            "по тому, что вышло в колонках?",
        ],
        sessions=[
            Session(
                label="t = 0.0",
                model=MODEL,
                messages=_messages(),
                temperature=0.0,
                max_tokens=MAX_TOKENS,
                repeats=REPEATS,
                extra_body=PIN_PROVIDER,
                note=(
                    "Жадное декодирование: на каждом шаге берётся самый "
                    "вероятный токен. Пять прогонов должны совпасть — но "
                    "«должны» тут не значит «обязаны»."
                ),
            ),
            Session(
                label="t = 0.7",
                model=MODEL,
                messages=_messages(),
                temperature=0.7,
                max_tokens=MAX_TOKENS,
                repeats=REPEATS,
                extra_body=PIN_PROVIDER,
                note=(
                    "Рабочий режим по умолчанию у большинства чат-моделей. "
                    "Названия расходятся, факт обычно ещё держится."
                ),
            ),
            Session(
                label="t = 1.2",
                model=MODEL,
                messages=_messages(),
                temperature=1.2,
                max_tokens=MAX_TOKENS,
                repeats=REPEATS,
                extra_body=PIN_PROVIDER,
                note=(
                    "Распределение сплющено, у хвоста появляется шанс. Пять "
                    "разных названий почти наверняка — и ровно здесь вторая "
                    "строка рискует поехать."
                ),
            ),
        ],
    ),
]
